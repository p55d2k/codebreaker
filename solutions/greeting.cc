#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    char name[1000] = {0};
    cin.getline(name, 1000);

    cout << name << " ni hao, xin nian kuai le!";

    return 0;
}