#include <bits/stdc++.h>
using namespace std;

int main() {
    int paper_number;
    cin >> paper_number;
    if (paper_number == 1) {
        cout << "Hello World";
    } else if (paper_number == 2) {
        long long a, b;
        cin >> a >> b;
        cout << a*b;
    }
    return 0;
}